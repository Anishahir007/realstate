<!doctype html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Properties - <%= site.title %></title>
  </head>
  <body>
    <h1>Properties</h1>
    <a href="<%= nav.home %>">Home</a>
    <ul>
      <% (properties || []).forEach(function(p){ %>
        <li><strong><%= p.title %></strong> — <%= p.city %>, <%= p.state %></li>
      <% }); %>
    </ul>
  </body>
  </html>


