<!doctype html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Terms & Conditions • <%= site.title %></title>
    <%- include('../partials/style.ejs') %>
  </head>
  <body>
    <div class="pc-shell">
      <nav class="pc-navbar">
        <div class="pc-container pc-navbar__row">
          <div class="pc-brand"><span><%= site && site.broker && site.broker.full_name || 'Broker' %></span><span class="pc-badge">ProClassic</span></div>
          <div class="pc-nav">
            <a class="pc-link" href="<%= nav.home %>">Home</a>
            <a class="pc-link" href="<%= nav.properties %>">Properties</a>
            <a class="pc-link" href="<%= nav.about %>">About</a>
            <a class="pc-link" href="<%= nav.contact %>">Contact</a>
          </div>
        </div>
      </nav>

      <main class="pc-container">
        <h1 class="pc-section__title">Terms & Conditions</h1>
        <div class="pc-card">
          <p class="pc-footer__muted">All listing information is provided as-is and subject to change without notice. We do not guarantee accuracy of third-party data. By using this site you agree not to misuse contact details or content and to comply with applicable laws.</p>
          <p class="pc-footer__muted">Transactions are subject to separate agreements and due diligence. For questions, contact <%= site && site.broker && site.broker.email || 'our team' %>.</p>
        </div>
      </main>

      <footer class="pc-footer"><div class="pc-container pc-footer__grid"><div class="pc-footer__muted">©</div></div></footer>
    </div>
  </body>
  </html>


